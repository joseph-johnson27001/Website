// Set timeout function for loading so that animations do not begin before page loading

setTimeout("showIt()", 300);

//Show function

function showIt() {
  document.getElementById("entrance").style.display = "block";
}
